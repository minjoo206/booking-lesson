// Temporary Firebase config that doesn't initialize to prevent connection errors
// This allows the UI to load without Firebase connection issues

export const auth = null;
export const db = null;
export default null;

